# Tanzeeb's `~/.config/nvim/init.vim`

## Installation

```sh
$ git clone git@github.com:tanzeeb/nvim.git ~/.config/nvim
```

Or

```sh
$ curl -o config.vim https://raw.githubusercontent.com/tanzeeb/nvim/master/init.vim
$ nvim -u config.vim
```

